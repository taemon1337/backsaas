FROM node:22-alpine
WORKDIR /app
COPY package.json ../../package.json /tmp/monorepo-root/
# Use pnpm inside container
RUN corepack enable && corepack prepare pnpm@9.0.0 --activate
WORKDIR /app
COPY . .
RUN pnpm install --prefer-offline || true
EXPOSE 3000
CMD ["pnpm","dev"]
